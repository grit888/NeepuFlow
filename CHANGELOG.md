# 更新记录

本项目的所有重要改动都会记在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-23

首个公开版本。

### 新增

- **Panabit 门户接口直连认证**：直接调用门户的 `setacct → login → ack_auth → stat` 接口链，
  不再依赖解析 HTML 表单（门户是 JS 单页应用，页面里没有 `<form>`）。
- **前端加密算法还原**：内置纯 Python 标准库实现的 AES-128-ECB，
  与门户 `encode()` **逐字节一致**，并附 FIPS-197 标准向量自测。
- **认证参数三级兜底**：探针 302 Location → 上次保存的门户地址 → 自造最小参数集，
  每一级都有网关可达性闸门，避免把手机热点的内网地址误当校园网地址提交。
- **tkinter 设置界面**：账号密码、运营商选择、开机自启开关、实时状态与日志。
- **后台常驻模式**（`--daemon`）：开机自启使用，无窗口、无托盘，内存约 38 MB。
- **开机自启**：写入「启动文件夹」的 `.vbs`，不写注册表、不装服务、不需要管理员权限。
- **凭据加密**：密码用 Windows DPAPI 加密存储，绑定当前用户与本机。
- **自检命令**（`--selftest`）：一次检查加密、凭据往返、联网、门户参数、自启状态。
- **断网重连测试**：先强制下线再自动认证，用于当场验证工具是否真的有效。
- **双启动器**：`启动.vbs`（无黑框）与 `启动.bat`（备用），均自动查找本机 Python，
  并跳过 Windows 应用执行别名的商店占位程序。

### 技术说明

- 密码提交格式：`hex(AES-128-ECB(ZeroPadding(4 位随机盐 + 明文)))`，每次提交都不同。
- 运营商字段取值来自门户 `api/pool.php`：移动 = `yidong`，联通 = `liantong`。
- 接口返回码：`0` 成功、`3/121/122` 已认证、`2` 重试、`4` 账号密码或运营商错误。

### 已知限制

- 仅支持 Windows（依赖 DPAPI 与启动文件夹机制）。
- 仅针对东北电力大学宿舍宽带（`NEEPU-STU`，Panabit 网关）。
  校园网 `NEEPU-WLAN`（学号认证）是另一套系统，本工具不涉及。
- 门户升级后接口或加密方式可能变化，届时需要同步适配。

[1.0.0]: https://github.com/
