# 技术原理

本文记录 NeepuFlow 是怎么把「必须点浏览器」的认证流程做成可自动化的程序的。
如果你要适配自己学校的认证门户，这份记录可以直接当路线图用。

---

## 0. 先说结论：这类门户，抓表单那条路是死的

大多数教程讲自动认证，套路都是「`requests.get` 拿页面 → 用正则/BeautifulSoup 找 `<form>` →
凑出 `username`、`password` 字段 → POST 上去」。

**这套做法对现代认证门户完全无效。**

东北电力大学宿舍宽带的认证网关是 **Panabit**，门户前端是它自带的 `raasportal` ——
一个纯 JS 单页应用（SPA）。你把 `view-source` 打开会发现：

- 正文里**没有 `<form>`**，只有一个空的挂载点；
- 所有输入框都是 JS 拉模板渲染出来的；
- 点「登录」时**并不是提交表单**，而是 `XMLHttpRequest` 打 JSON 接口；
- 密码**在提交前被前端加密**，服务端只收密文。

所以第一步不是写代码，而是**换路线**：直接调门户自己的接口。

> 判断口诀：如果页面里有 `<div id="app">`、`portal_config`、`new xxxportal()`、
> `assets/js/` 下一堆框架文件，而正文几乎是空的 —— 那就是 SPA 门户，走本文路线。

---

## 1. 抓全静态资源

门户 HTML 只引用 JS，JS 又动态加载模板和逻辑脚本，**要顺着引用链抓两三层**：

```
portal.html                 ← 入口，含 config 与 bootstrap JS
assets/js/*.js              ← 框架 / 加密库 / 接口库
<theme>/js/index.js         ← 真正的页面逻辑（提交、校验、加密都在这）
<theme>/                    ← 表单 HTML 模板（JS 拉取后注入 DOM）
```

用 Python `urllib` 抓即可，但**必须带 `Referer` 和浏览器 UA**，很多网关会拒。

在入口 JS 里搜这些关键词定位模板路径：`$.get({url:...})`、`appendChild(script)`、
`'tp/' + ...`、`pathname`。

---

## 2. 定位接口清单

在接口库 JS 里搜 `url:`、`$.post`、`fetch(`、`ajax`、`.php`、`.do`、`api/`，
就能拉出一张接口表。本项目用到的：

| 接口 | 作用 |
|---|---|
| `api/pool.php` | 取运营商列表（`yidong` / `liantong` …） |
| `api/setacct.php` | 提交账号 |
| `api/login.php` | 发起认证，**密码密文从这里进** |
| `api/ack_auth.php` | 确认认证 |
| `api/stat.php` | 轮询认证状态 |
| `api/logoff.php` | 下线（「断网重连测试」用的） |

> ⚠️ **这些接口在「已经联网」时通常全部不可用**（网关会 502 / 拒绝连接），
> 因为它们只在「未认证」状态下才对外服务。
> 所以运行时判断顺序**必须**是「先测联网，在线就收工」，别拿它当网络探针。

---

## 3. 还原前端的 `encode()`

页面逻辑里提交前有这么一行：

```js
data.pass = encode(data.pass);
```

顺链找 `encode` 的定义，会落进一个**被混淆过的 `crypto.js`**。

### 不要去读混淆代码，直接把它跑起来

被混淆的加密函数之所以能还原，是因为**它必须能被同页面的其它 `<script>` 调用**
—— 也就是说它挂在全局作用域上。所以造个 sandbox 跑它就行了：

```js
const vm = require('vm'), fs = require('fs');
const sandbox = {
  console, Math, Date, JSON, String, Number, Array, Object, RegExp, Error,
  parseInt, parseFloat, isNaN, encodeURIComponent, decodeURIComponent,
  navigator: { userAgent: 'Mozilla/5.0', appName: 'Netscape', platform: 'Win32' },
  document: { cookie: '', createElement: () => ({ style: {}, setAttribute(){}, appendChild(){} }),
              getElementsByTagName: () => [], writeln(){}, write(){} },
  location: { href: 'http://1.2.3.4/', protocol: 'http:', host: '1.2.3.4', pathname: '/' },
  screen: { width: 1920, height: 1080 },
};
sandbox.window = sandbox; sandbox.self = sandbox;
sandbox.top = sandbox; sandbox.parent = sandbox;
vm.createContext(sandbox);
vm.runInContext(fs.readFileSync('crypto.js', 'utf8'), sandbox, { filename: 'crypto.js' });

// ① 直接拿到混淆函数的源码
console.log(sandbox._0x5e74e9.toString());

// ② 把字符串表整个 dump 出来 —— 混淆的字符串常量一目了然
const map = {};
for (let i = 0; i < 0x100; i++) {
  try { const v = sandbox._0x500d('0x' + i.toString(16)); if (typeof v === 'string') map['0x'+i.toString(16)] = v; } catch (e) {}
}
console.log(JSON.stringify(map, null, 1));
```

字符串表 dump 出来后，密钥、字符集、`ECB` / `ZeroPadding` 这些模式名就直接躺在那儿了
（混淆器会把 `CryptoJS.AES` 改名成 `CryptoJS.VDX` 这类，靠没被改的字段名
`encrypt` / `decrypt` / `keySize` 认出来）。

### 本项目还原结果

```
encode(pw) = hex( AES-128-ECB( ZeroPadding( 4 位随机盐 + 明文 ), key ) )
```

- 盐：4 位随机字符，字符集 `A-Za-z0-9`（所以**每次提交的密文都不同**，这是防重放）
- 模式：ECB + 零填充
- 密钥：固定 16 字节，从字符串表里直接读到

---

## 4. 用纯 Python 复刻，并**逐字节验证**

本机 `pip` 常常又慢又超时，所以能不引第三方库就不引。
AES-128 用纯 Python 写也就 120 行左右（S-box + 密钥扩展 + ShiftRows / MixColumns），
一次登录只算一个块，性能完全够。

**写完必须逐字节比对，不能「看起来对」：**

```python
# 先在 Node 里用门户自己的实现算出基准，再比 Python 的
assert aes_ecb_hex(b'abcdTestPass123', KEY) == 'aed1cc13e22b30f6ca953b296a6d6d03'

# 再拿 FIPS-197 官方标准向量兜底
assert aes_ecb_hex(bytes(16), bytes.fromhex('000102030405060708090a0b0c0d0e0f')) \
       == 'c6a13b37878f5b826f4f8162a1c8d879'
```

两个都对上，才敢写进程序。`--selftest` 里跑的就是这两条。

---

## 5. 用假账号验证接口格式（不碰真实密码）

**这一步是分水岭。** 拿一个绝对不存在的账号去打登录接口：

```
user=nobody_test_zzz  pass=<32 位随机 hex>  ...
```

看返回：

| 返回 | 结论 |
|---|---|
| 「账号不存在 / 手机号或宽带帐号不正确」 | **格式全对**，服务端已经拿着我们的输入去查库了，链路通 |
| `ARG` / `参数不正确` / 500 | 字段名或 URL 参数不对，继续调 |
| 无响应 / 502 | 多半是「其实已经联网」，或者不在该网络里 |

这一步能在**完全不接触真实凭据**的前提下，证明 URL 参数、字段名、body 结构都正确。
务必先做这一步再拿真账号试。

---

## 6. URL 上那串参数从哪来

多数网关会把外网请求 302 到门户，并在 `Location` 上带一串会话参数：

```
http://<门户>/?wlanuserip=<本机IP>&clientip=<本机IP>&wlanacname=<厂商>
             &clientmac=<本机MAC>&paip=<公网IP>&vlan=<vlan>&iarmdst=<原目的地>
```

**最可靠的办法就是抓这个 302 的 `Location`**（探针不要跟随重定向）。
首选探针是 `http://www.msftconnecttest.com/redirect` —— Windows 自己就用它，
网关拦它拦得最勤。

三级兜底，从准到不准：

1. **探针 302 Location** —— 自带全部参数，最准。同时兼容「页面被直接换成门户」（无 302，正文即门户页）的情况。
2. **沿用上次保存的完整门户 URL** —— 参数里只有 `clientip` / `wlanuserip` 随会话变，其余（vlan / paip / mac）几乎固定。
3. **自造最小集** —— `wlanuserip` + `clientip` + `wlanacname`。

> ⚠️ **第 3 级必须加闸门**：先用 TCP `connect_ex()` 确认网关真的可达，再决定要不要用本机当前 IP。
> 否则人会连手机热点（典型 `192.168.43.x`），程序把热点地址当校园网地址提交上去 —— 必错。

---

## 7. 运行时的判断顺序（很重要）

```
① SSID 是不是目标网络？  不是 → 什么都不做
② 已经联网了吗？         是   → 收工（门户接口此时不可用）
③ 探测门户参数
④ 发起认证，轮询 stat
```

顺序错了会得到一堆 502，然后误判成「工具坏了」。

---

## 8. 接口返回码

| ret | 含义 | 处理 |
|---|---|---|
| `0` | 认证成功 | 完成 |
| `3` / `121` / `122` | 已经认证过了 | 当成功 |
| `2` | 正在认证，需重试 | 等一会再轮询 |
| `4`（及 `5`） | 账号密码错 **或者运营商选错** | 报错停机 |

**运营商字段选错，返回的往往就是「账号或密码不正确」** —— 这一点极具误导性，
排查「密码明明没错却报密码错」时，第一嫌疑永远是运营商标识。

---

## 9. 踩坑记录

- **`.bat` 必须 CRLF**，否则 `cmd` 解析带括号的 `for` 块会失败。
- **`.vbs` 必须存成 ANSI(GBK)**：`wscript` 对**没有 BOM** 的文件按 ANSI 解码，
  存 UTF-8 会让中文变乱码；而**带 BOM 的 UTF-8 直接解析失败**。
- **`Sub` 是 VBScript 保留字，不能当变量名**（`Dim sub` → `800A03F2 缺少标识符`），
  而且这个错**只在执行到那一行时才报**，不是加载即报，二分法很容易漏。
- **`WScript.Shell.Exec` 不展开 `%ComSpec%`** —— 它内部走 `CreateProcess`，
  不做环境变量替换，必须自己 `ExpandEnvironmentStrings("%ComSpec%")`；
  否则报错会被 `On Error Resume Next` 静默吞掉，表现为「这条查找路径永远失效」。
- **从 PATH 找 `pythonw.exe` 要排掉 `WindowsApps`** —— Windows 11 的「应用执行别名」
  会预置一个商店占位程序，命中后双击会弹出微软商店。
- **`CryptProtectData` / `CryptUnprotectData` 参数个数不同（7 vs 6）**，
  图省事共用一个参数列表会让 ctypes 读越界 → 进程 `0xC0000409` 静默闪退、零输出。
  排查靠 `python -u`。
- **抓页面时 `Referer` + UA 都要带**，很多网关会拒。
- **开机自启写在启动文件夹**，`sh.Run(..., 0, False)` 起 `pythonw.exe`：
  不弹黑框、不碰注册表。代价是**挪动目录后必须重建**（路径写死在 `.vbs` 里）。
